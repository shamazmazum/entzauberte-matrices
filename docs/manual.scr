@begin[ref=index](section)
   @title(Overview)
   Entzauberte (something like "disenchanted") matrices is a lightweight wrapper
   around LAPACK. It can add/multiply/invert matrices, has a solver for systems
   of linear equations and can perform basic matrix decompositions.

   In contrast to MAGICL, entzauberte-matrices operates on usual Common Lisp
   arrays with real or complex floating points elements, more precisely on
   arrays of type @c((array TYPE 2)) where @c(TYPE) is one of
   @c(single-float)/@c(double-float)/@c((complex single-float))/@c((complex
   double-float)). For binary operations, both arguments must have the same
   type.

   Currently, entzauberte-matrices is SBCL only library. It requires OpenBLAS to
   work.
@end(section)

@begin(section)
   @title(API)
   @cl:with-package[name="entzauberte-matrices"](
       @cl:doc(function dot)
       @cl:doc(function norm)
       @cl:doc(function add)
       @cl:doc(function sub)
       @cl:doc(function scale)
       @cl:doc(function mult)
       @cl:doc(function @)
       @cl:doc(function det)
       @cl:doc(function invert)
       @cl:doc(function eig)
       @cl:doc(function eig-self-adjoint)
       @cl:doc(function svd)
       @cl:doc(function transpose)
       @cl:doc(function solve)
       @cl:doc(function reshape)
       @cl:doc(function reshape-unsafe)
       @cl:doc(function vector->column)
       @cl:doc(function vector->column-unsafe)
       @cl:doc(function vector->row)
       @cl:doc(function vector->row-unsafe)
       @cl:doc(function hstack)
       @cl:doc(function vstack)
       @cl:doc(function set-num-threads)
     )
@end(section)
